# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'HeroCoordinatorTest' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for HeroCoordinatorTest
  pod 'SnapKit', '~> 5'
  pod 'Hero', '~> 1.4'
  pod 'RxSwift', '~> 4.4'
  pod 'RxCocoa', '~> 4.4'

  target 'HeroCoordinatorTestTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
